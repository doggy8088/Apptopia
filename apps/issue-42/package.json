{
  "name": "project-skill-agent-analyzer",
  "version": "0.1.0",
  "description": "CLI tool to analyze project docs, map capabilities, compare registries, and generate skill/agent definitions",
  "private": true,
  "type": "commonjs",
  "bin": {
    "project-orchestrator": "dist/index.js"
  },
  "scripts": {
    "start": "npm run build --silent && node dist/index.js",
    "build": "tsc -p tsconfig.json",
    "lint": "tsc --noEmit -p tsconfig.json",
    "test": "vitest run"
  },
  "dependencies": {
    "yaml": "^2.6.1"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "typescript": "^5.7.2",
    "vitest": "^2.1.8"
  }
}
